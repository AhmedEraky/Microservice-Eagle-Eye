spring:
  application:
    name: licensingservice
  profiles:
    active: dev
  cloud:
    config:
      uri: http://localhost:8888
eureka:
  instance:
    prefer-ip-address: true
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      serer-one: http://localhost:8761/
